<Window x:Class="GuidedTour.Wpf.GuidedTourPresenterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GuidedTour.Wpf"
        mc:Ignorable="d"
        x:Name="ThisWindow"
        d:DesignHeight="450" d:DesignWidth="800" AllowsTransparency="True" WindowStyle="None" Background="Transparent" ShowInTaskbar="False"
        
        Left="{Binding ElementName=ThisWindow, Path=Screen.Left}"
        Top="{Binding ElementName=ThisWindow, Path=Screen.Top}"
        Height="{Binding ElementName=ThisWindow, Path=Screen.ActualHeight}"
        Width="{Binding ElementName=ThisWindow, Path=Screen.ActualWidth}" Loaded="ThisWindow_Loaded"
        
        >
    <Grid>
        
    </Grid>
</Window>
